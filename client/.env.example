# Base URL of the PlacementOS API.
# In dev, leave as /api to use the Vite proxy → http://localhost:5000
# In prod (Vercel), set to your Render URL, e.g. https://placementos-api.onrender.com/api
VITE_API_URL=/api
VITE_APP_NAME=PlacementOS
